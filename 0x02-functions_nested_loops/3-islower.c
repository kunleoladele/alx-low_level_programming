#include "main.h"

/**
 * _islower - check if character input "a" is lowercase if YES display 1
 * another case display 0
 *
 * c: character in lowercase
 *
 * Return: 1 for lowercase, otherwise return 0
 */
int _islower(int c)
{
int c;
if (c >= "a" && c <= a)
{
return(1)
}
else
{return(0)
}

{
_putchar('\n');
}
